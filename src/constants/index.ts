import ConstPath from "./ConstPath"
import ConstNumber from "./ConstNumber"
import ConstOnline from "./ConstOnline"
import TouchMode from "./TouchMode"

const DEFAULT_USER_NAME = 'Customer'
const DEFAULT_LANGUAGE = 'CN'

export {
  ConstPath,
  ConstNumber,
  ConstOnline,
  TouchMode,

  DEFAULT_USER_NAME,
  DEFAULT_LANGUAGE,
}